{
  "installCommand": "corepack enable && corepack prepare pnpm@9.0.0 --activate && pnpm install",
  "buildCommand": "pnpm run build",
  "outputDirectory": "artifacts/web3-marketplace/dist",
  "build": {
    "env": {
      "NODE_VERSION": "20.11.0"
    }
  }
}
